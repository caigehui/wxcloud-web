// import React from 'react';
// import { KeepAlive, useModel } from 'umi';

// const keepAlive = (Component: React.FC<{ menu: Array<any> }>, name: string) => () => {
//   const { menu } = useModel('useAuthModel');
//   return (
//     <KeepAlive name={name} saveScrollPosition="screen">
//       <Component menu={menu} />
//     </KeepAlive>
//   );
// };

// export default keepAlive;
